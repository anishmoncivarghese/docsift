[project]
name = "docsift"
version = "0.1.0"
description = "Convert documents once. Give agents only what they need."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Anish Monci Varghese" }]
dependencies = [
  "typer>=0.12",
  "pydantic>=2.7",
  "tiktoken>=0.8",
]
keywords = ["pdf", "markdown", "docling", "markitdown", "rag", "llm", "document-conversion"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Text Processing :: Markup :: Markdown",
]

[project.optional-dependencies]
markitdown = ["markitdown[docx,pptx,xlsx,pdf]>=0.1.1"]
docling = ["docling>=2.0"]
all = ["docsift[markitdown]", "docsift[docling]"]

[project.scripts]
docsift = "docsift.cli.main:app"

[project.urls]
Homepage = "https://github.com/anishmoncivarghese/docsift"
Repository = "https://github.com/anishmoncivarghese/docsift"
Changelog = "https://github.com/anishmoncivarghese/docsift/blob/main/CHANGELOG.md"

[dependency-groups]
dev = [
  "pytest>=8.0",
  "ruff>=0.5",
  "fpdf2>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/docsift"]

[tool.hatch.build.targets.sdist]
include = [
  "src",
  "tests",
  "README.md",
  "LICENSE",
  "CHANGELOG.md",
  "pyproject.toml",
]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.flake8-bugbear]
# Typer's documented pattern: typer.Argument()/typer.Option() as parameter
# defaults are how the CLI declares its options; they aren't mutable-default bugs.
extend-immutable-calls = ["typer.Argument", "typer.Option"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: needs real conversion engines (may download models)"]
addopts = "-m 'not integration'"
